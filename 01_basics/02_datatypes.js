"use strict"; // treat all JS code as newer version.

// alert(3+3) // we are using nodejs not browser

console.log(3+3)
console.log("Adesh")

let name = "Adesh" 
let age = 29 
let isLoggedIn = false
let state = null
let x = undefined

console.table([name, age, isLoggedIn, typeof age, 
    typeof name, typeof isLoggedIn, typeof state, typeof x])

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique

// object 

// console.log(typeof age)
